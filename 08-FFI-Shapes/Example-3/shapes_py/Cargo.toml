[package]
name = "shapes_py"
version = "0.1.0"
edition = "2024"

[lib]
name="shapes_py"
crate-type = ["cdylib", "rlib"]

[dependencies]
shapes = { path = "../shapes" }

[dependencies.pyo3]
version = "0.29.0"
features = ["extension-module"]
